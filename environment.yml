name: saltproc-env
channels:
  - conda-forge
  - defaults
dependencies:
  - numpy
  - openmc>=0.13.3
  - pytables
  - networkx
  - pydot
  - pytest
  - jsonschema
  - pip:
    - serpentTools
    - argparse==1.4.0
