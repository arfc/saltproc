name: saltproc-env
channels:
  - conda-forge
  - defaults
dependencies:
  - numpy
  - openmc
  - pytables
  - networkx
  - pydot
  - pytest
  - jsonschema
  - pip:
    - argparse==1.4.0
    - serpentTools
